Data requirements
Data will be travel and hospitality related and will be loaded by the participants.

What are the primary goals you aim 
Agentic data engineering using openflow and dbt.

Special Request or Notes
Each demo account has an: an openflow deployment and a runtime, a postgres instance that is accessible to openflow as well as external network access. During the lab, the participants will install the postgres connector to load data from the postgres instance to Snowflake. Then use dbt to transform that data and set up data quality tests. The participants will use a script to load 
